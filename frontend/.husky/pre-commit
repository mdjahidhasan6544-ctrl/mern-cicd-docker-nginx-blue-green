#!/usr/bin/env sh
# Pre-commit: run lint-staged on staged files only (fast).
# Configured in /.lintstagedrc.json at repo root.
. "$(dirname -- "$0")/_/husky.sh"

cd "$(git rev-parse --show-toplevel)"

npx --no-install lint-staged
